pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
    plugins {
        // Don't upgrade to 8.3 and above
        id 'com.android.application' version '8.2.2b' apply false
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'
